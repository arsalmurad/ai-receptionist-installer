## What changed and why

## How it was tested

- [ ] `npm run typecheck`
- [ ] `npm run lint`
- [ ] `npm test`
- [ ] `npm run build -w apps/web`
- [ ] Ran it (`npm run demo` or against a real deployment) and actually used the changed feature, if this touches `apps/web`

## Checks affected (if any)

If this adds or changes a `frontdesk verify` or `frontdesk check` gate, name it here and confirm it has a vitest test.

## Notes for the reviewer

Anything that needs a second look, or that you're unsure about.
